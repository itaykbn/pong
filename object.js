
/**the object class is asort of container for valus it contains the values of every 
 instance created by it  and alowes to acsess these values at any given moment
 it helps organize the code and keep things more clear and incapsulated*/

class Obj{
    // class constructor
    constructor(x, y, w, h, speedY, speedX) {
        this.x = x;
        this.y = y;
        this.w = w;
        this.h = h;
        this.speedX = speedX;
        this.speedY = speedY;
    }

}